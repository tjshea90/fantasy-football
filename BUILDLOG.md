2026-09-01T21:34Z | step 1/11 | step 0: bundle skeleton, spec, ladder, checkpoint system
2026-09-01T21:36Z | step 3/11 | steps 1-2: seed.json (170 players validated) + scoring engine, 33/33 tests pass
2026-09-01T21:39Z | step 5/11 | steps 3-4: espn.js feed parser + store.js data model, both smoke-tested
2026-09-01T21:42Z | step 6/11 | step 5: full UI (live/lineups/rosters/standings/data), ES2018 guard added
2026-09-01T21:44Z | step 8/11 | steps 6-7: Android shell + Gradle-free build. FIRST WORKING APK, no native libs, minSdk29/targetSdk36
2026-09-01T21:46Z | step 10/11 | steps 8-9: advice engine seeded with league-scored 2026 projections, playoff bracket, RESUME card
2026-09-01T22:12Z | step 10/11 | v1.1 FIX: seed shipped as seed.js script, not an XHR (file:// origin cannot fetch siblings). Boot regression test + on-screen error handler added.
2026-09-01T22:21Z | step 11/12 | v1.2: status-bar inset fix, player search over 785-player DB with ESPN refresh, hardened saves (.bak + auto-backup to Downloads), APK now travels inside the checkpoint zip
2026-09-01T22:26Z | step 11/12 | v1.3: status bar fixed properly — WebView stays full-bleed, insets pushed into CSS vars, plus a manual Screen fit override on the Data tab
2026-09-01T22:32Z | step 11/12 | v1.4: ESPN abbr map (wsh) + retry fixes the ARI/WAS roster failures; in-app modal replaces the file:// alert; feed VERIFIED correct on-device
2026-09-01T22:40Z | step 11/12 | v1.5: 60px tab targets, global progress bar that survives tab switches, per-tab scroll memory, injury cache persisted, ARI fixed via numeric team id
2026-09-01T22:58Z | step 11/12 | v1.6: root-caused ARI — large bodies were being dropped crossing the JS bridge. Chunked transfer + real error reasons.
2026-09-01T23:02Z | step 11/12 | v1.7: five route shapes per team with a shape-tolerant parser, plus a per-team route diagnostic. ARI's 404 is network-specific, not a URL bug.
2026-09-02T02:20Z | v1.8 | step 12/13 | v1.8: plain version numbers; per-position scoring audit on-device + two real feed bugs fixed (pick-six double count, uncredited 2PTs); lineups auto-default for all 10 teams with manual picks protected; live auto-refresh with a pinned my-matchup card; advice rebuilt on four re-scored projection sources plus optional Claude reasoning over live news
2026-09-02T03:17Z | v1.9 | step 12/13 | v1.9: BRIEF.md gains a CACHE DISCIPLINE section — the prompt-caching mechanism behind the usage rules, so a future session knows why not to dump source into the reply. Docs only; APK rebuilt for version consistency
2026-09-02T03:23Z | v2.0 | step 13/14 | v2.0: FIXES THE FREEZE. The JS bridge was synchronous, so every request blocked the renderer thread — no repaint, no progress bar, no taps — and the Claude call made it look like a crash. Bridge is now async (httpAsync + __httpDone) with per-request timeouts and an elapsed-time display
2026-09-02T03:31Z | v2.1 | step 14/15 | v2.1: from the first on-device run. Claude call now streams (a silent minute-long POST was being closed on cellular: unexpected end of stream) plus Connection:close and a zero-byte retry. Projections ask for an explicit scoring period, routes judged on weekly coverage, and any rostered player with no week line is named in the UI
2026-09-02T03:35Z | v2.2 | step 14/15 | v2.2: Claude spend meter. No API can report a key's balance without an org-wide admin key, so the app prices every call from the usage each response reports and keeps a local ledger: spend, remaining against a budget you enter, per-sync cost, full history. Found and fixed a real bug doing it — streamed usage was being replaced rather than merged, dropping input tokens
2026-09-02T18:49Z | v2.2 | step 15/21 | v2.3: sync efficiency (final games cached, 3-at-a-time fetch), feed-shape canary, league book
2026-09-02T18:49Z | v2.3 | step 15/21 | v2.3: sync efficiency (final games cached, 3-at-a-time fetch), feed-shape canary, league book
2026-09-02T18:53Z | v2.4 | step 16/21 | v2.4: Claude cost — triage (settled/research/carried), searches sized to the work, cached prompt prefix, depth switch
2026-09-02T18:57Z | v2.5 | step 17/21 | v2.5: sim.js — live win probability, playoff/title odds, power rankings, luck index, bench regret, all in league points
2026-09-02T19:00Z | v2.6 | step 18/21 | v2.6: value.js — free-agent board in league points, upgrade finder, usage trends, VORP trade evaluator
2026-09-02T19:03Z | v2.7 | step 19/21 | v2.7: lineup alerts — pure-Java pre-kickoff check (bye / OUT / empty slot) with the app closed, no exact-alarm permission
2026-09-02T19:07Z | v2.8 | step 20/21 | v2.8: weekly recap + share, full-season schedule generator, Sleeper as a gap-filling second projection source
2026-09-02T19:08Z | v2.9 | step 21/21 | v2.9: hardening — per-card and per-screen error isolation, offline told apart from a broken feed, ES2018 guard over every asset, release notes
2026-09-02T19:14Z | v3.0 | step 22/22 | v3.0: FIXES THE LAUNCH CRASH. build.sh was discarding javac's exit status, so v2.7-v2.9 shipped without Alerts and NativeBridge. Compile failure is now fatal and ship.sh verifies the dex
2026-09-03T16:08Z | v3.1 | step 23/28 | v3.1 TOUCH + SCROLL. Bottom tabs: min-height 60->88px, icon 17->24px, label 12->13px at weight 600, symmetric padding so the glyph is optically centred; body and toast clearances moved 96->124px. THE DROPDOWN JUMP, fixed at the right layer: render() ended with scrollTo(scrollMem[view]) and scrollMem is written ONLY by the tab handler, so every select-driven repaint snapped to wherever that tab was last opened - the top. render() now holds the exact current offset on a same-view repaint, restores the remembered one only on a real tab switch, skips the call entirely when already within 2px, and hands focus back to the control you used via data-fk + preventScroll. Every dropdown in the app was affected, not just Lineups. renderTop() kept for week change, import, reset. ALSO: ship.sh could silently re-ship an existing version number - the zip-exists test only sees the local output dir, so a resumed session in a fresh container re-shipped v3.0 over the v3.0 Tj already had. It now also checks BUILDLOG.md, which travels in the zip, and refuses to ship an APK stamped with a different version than the zip. 12 new regression assertions.
2026-09-03T16:10Z | v3.2 | step 24/28 | v3.2 FREE AGENTS BY POSITION. The board showed only quarterbacks and the data was not at fault: freeAgents() sorts every position together by league points, and because this league pays 1 point per completion a startable QB is worth about twice a startable RB, so one global sort puts every QB on top. Added Value.byPos (a bucket per position, each sorted and capped, every row carrying value over the best free agent at its own position) and Value.byVor (the one mixed ranking that is comparable across positions). The board opens on All positions with the best 6 at each, a chip per position for a 30-deep list, and a Best value chip. 13 new tests, one of which reproduces the old all-QB behaviour on purpose.
2026-09-03T16:13Z | v3.3 | step 25/28 | v3.3 CLAUDE MODEL PICKER + one more cost cut. The model field was free text, which cannot tell a typo from a retired model id - both are the same 404 mid-sync. A hard-coded list would go stale instead, so the list is fetched from GET /v1/models?limit=1000 with the key already on the Data tab, sorted newest first and cached in settings so the picker works offline; every picker ends in Custom so a model newer than the cache is still reachable. Both the main and the routine model are pickable, built by one modelPicker() function. The weekly recap now always uses the routine model - it is the only Claude call with no judgement in it (facts are computed on the phone and handed over verbatim), so this costs nothing in accuracy. ALSO ship.sh now re-stamps the APK itself when a version bump makes it stale, instead of failing and asking for the same two commands again. 10 new assertions.
2026-09-03T16:16Z | v3.4 | step 26/28 | v3.4 FREE-AGENT CLAUDE SYNC. Ask Claude about the wire, on the Free agents card. The app decides who is available and what he is worth in THIS scoring - it is the only thing that knows either - and Claude adds only what a stat line cannot show (the starter ahead of him got hurt, a rookie took the job) then re-ranks the shortlist for this roster. That split is also the cost control: the pool sent is the top 6 per position rather than the 600-player wire, and the search budget is sized to the positions of need (starters within 4 points of the best free agent at their own position), two searches each, capped at 10. The fixed half of the prompt carries no week or date so it actually caches, asserted by a test. Results cached to disk, grouped by position, a previous week's read labelled as stale, and any player Claude names who was not in the pool we sent is flagged unverifiable and gets no Add button. 12 new tests.
2026-09-03T16:17Z | v3.5 | step 27/28 | v3.5 ADVICE SOURCING. The blend claimed multiple sources but only one was an outside professional projection: Sleeper was fetched and then discarded whenever ESPN already had a week line, so it only ever patched holes. Two projections that disagree are exactly what a blend is for, so Sleeper is now kept in its own field alongside ESPN's and weighted as an independent source (2.0 against ESPN's 3.0, lower only because ESPN's line is opponent-aware); it still fills gaps where ESPN has nothing, and the diagnostic now reports gaps filled AND second opinions given. The conversion is stated on screen: every outside projection is half-PPR standard where a completion is worth nothing and here it is worth a point, so their stat line is imported and re-scored and their own projected points are never used for anything. The per-player breakdown says how many outside projections contributed and that they were converted. 7 new assertions.
2026-09-03T16:29Z | v3.6 | step 28/28 | v3.6 REVIEW + OPTIMISE. Full read of all twelve JS modules. Nine correctness defects fixed, the worst being that a hand-entered adjustment was wiped by every sync including the 45s live poll (it is the only way to score the three longest-play bonuses, so it lost real points weekly), and that a dropped player's id was reused and inherited his stat history because S.stats is keyed by pid and is not cleaned on a drop. Also: Sim.season double-counted every unscored week; Ai.listModels could never succeed without raw:true; weeksLeft counted weeks already played and inflated trade values ~60%; an empty opponents map is truthy and froze out the good one; esc() did not escape; three lineup-mutating paths never invalidated the sim cache. Performance: playerById is an index instead of ~170 allocations per call (the League tab was doing close to a million), the 785-player free-agent scan is memoised, usage strings are lazy. AND: three blocks of my own assertions from v3.1/v3.3/v3.6 had been appended below process.exit and never ran - all pass now that they do, and a guard asserts no assertion is ever stranded there again. 274 assertions across four suites, all green. Four known-open items documented in STATE.
2026-09-03T16:34Z | v3.7 | step 29/32 | v3.7 MODALS + SAFE IMPORT. All six confirm()/prompt() sites replaced with real in-app dialogs. Two of them were the manual backup and restore path and that was the actual bug: prompt()'s default value is a single-line field that truncates and is not reliably selectable, so Copy this backup handed back an unusable string for a mid-season export of tens of thousands of characters, and Paste a backup JSON could not take one. textModal uses a real textarea, prefilled and preselected. Also importJSON assigned S = o and THEN called save(), which touches S.settings.savedAt, so a backup with no settings key threw AFTER the live season had already been replaced in memory while the catch reported a clean failure; it also skipped init()'s settings migration so an old backup came back missing keys. It now validates the shape and every team, migrates onto the candidate, and swaps only at the end. 9 new assertions including an outright ban on confirm( and prompt(.
2026-09-03T16:36Z | v3.8 | step 30/32 | v3.8 THE HOT PATHS. Scoring.score() is memoised on the line object rather than gaining a second arithmetic-only implementation - the scoring engine is the product and two copies means one drifts. Keyed on manualAdj plus the three weekly bonus flags, the only fields edited after parse, and non-enumerable so it never lands in a save. defenseProfile memoised on week+generation: autoFillWeek called it once per team for an identical answer, on boot, on every week change and on every 45s live poll. Lognormal sigma cached instead of recomputed 80,000 times per matchup. Roster search builds an owner index instead of re-normalising 170 names per hit per keystroke (~3,400 regex calls per character). Four one-line duplications: double recap build, bench-regret rescan, slotKeys inside a loop, played-weeks per row. 12 new assertions.
2026-09-03T16:39Z | v3.9 | step 31/32 | v3.9 THE TRANSPORT. Chunk reassembly was up to 8192 back-to-back synchronous binder calls inside the promise resolver on the renderer thread - the exact freeze httpAsync exists to prevent, for precisely the large bodies it was built for. Batches of 8 now yield to the event loop. The legacy synchronous branch carried a second copy of the same loop with a different guard (4096 vs 8192) and no raw support; it calls parseBody like everything else now. A malformed chunk header made parseInt yield NaN, which made both the loop condition and the truncation check false, so it died in JSON.parse('') naming neither the bridge nor the header; now guarded. Field-goal kicker names took the whole left-hand side of the play text so a (12:34) J.Tucker byline never matched; prefixes stripped, surnames compared. Honest scope: 3 of 4 realistic formats already matched, only the initial-and-surname form failed, so this was one format not every kicker. 9 new assertions.
2026-09-03T16:43Z | v4.0 | step 32/32 | v3.10 DEAD CODE + ERROR PATHS + AN INTEGRATION SUITE. Removed the write-only twoPts collector, an unused slice, an unused parameter and the unreachable 'not built yet' advice placeholder. Two review findings did not survive contact: bookNames() is NOT dead (test_engine.js is its caller) and went back with a comment; priceOf's unused parameter was real but its body used it as a default, so it declares rates locally now. syncAll no longer abandons the injury feed, projections and Claude when the schedule endpoint fails. Both raw alerts bridge calls are guarded. Best value degenerated to all-QB again under thin data - with no projections every vor ties at 0 and a plain sort returns bucket order - so ties now break on rank within position. NEW tools/test_integration.js, 40 checks: boots all thirteen modules into one window in index.html order over the real database and drives real work through it, because every v3.6 defect was cross-module and no module's own tests could have caught them. AND ship.sh never ran the tests at all; all five suites are now a gate.
2026-09-03T16:58Z | v4.1 | step 33/33 | v4.1 PLAYER IDENTITY. Kenneth/Kenny Gainwell were two different men to every identity check. The listing Tj saw was the mildest face: the weekly sync indexed rosters by exact normalised name, so an ESPN box score under the other spelling matched nobody and he scored 0.0 silently. The obvious fix (same surname + position + team) is a disaster on this data - Bijan Robinson and Brian Robinson Jr. are both Atlanta RBs, Elijah Moore and D.J. Moore are both Buffalo WRs - and team cannot even be required because the Gainwell copies disagreed on it. Of 65 same-surname pairs in the real data exactly ONE is the same man, so names.js uses a curated first-name nickname table plus a short alias list, folding only the first token. The dedupe then exposed a second bug: the database held Gainwell and Okonkwo twice with conflicting teams, meaning a wrong bye week; both sourced and fixed at the data level, and PlayerDB now reports any that reappear instead of picking silently. New tools/test_names.js, 19 checks, mostly about REFUSING to merge, with the full merge set over the real data pinned.
2026-09-05T17:18Z | v4.2 | step 34/34 | v4.2 invisible auto-backups: rotated app-private snapshots (full state, matchups included) replace the Downloads-clutter export path; in-app restore picker added
2026-09-07T19:01Z | v4.3 | step 34/34 | v4.3 WIP — three reported bugs fixed, Claude-app round trip, background sleep
2026-09-07T19:23Z | v4.5 | step 43/43 | v4.5 — three reported bugs fixed, Claude-app round trip on both tabs, kickoff times + pre-Sunday alerts, the app sleeps when backgrounded, network/caching sweep
